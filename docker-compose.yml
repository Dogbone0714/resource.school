version: '3.8'

services:
  # FastAPI 後端
  backend:
    build: ./backend
    container_name: resource_school_backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=mysql+pymysql://root:0h96Laxmn4Q57N2XBj8oepU1ysO3ErCT@cgk1.clusters.zeabur.com:32188/zeabur
    volumes:
      - ./backend:/app
    networks:
      - resource_school_network

  # React 前端
  frontend:
    build: ./frontend
    container_name: resource_school_frontend
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=https://api.morago.com.tw
    volumes:
      - ./frontend:/app
      - /app/node_modules
    networks:
      - resource_school_network

networks:
  resource_school_network:
    driver: bridge
